/* eslint-disable no-unused-expressions */
/* eslint-disable no-undef */
// const chai = require('chai');
// const app = require('../index');

// const { expect } = chai;
// //chai.use(chai);

// describe('Index', () => {
//   it('Returns the Index of the app', () => {
//     chai
//       .request(app)
//       .get('/')
//       .end((res) => {
//         expect(res).to.have.status(200);
//       });
//   });
// });
